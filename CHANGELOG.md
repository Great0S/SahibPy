# Changelog

## Recent updates

* Added safer Cloudflare/hard-to-load page handling and a `FetchError` for fatal failures.
* Improved Turkish date parsing, including relative values such as `Bugün` and `Dün`.
* Added URL display in result tables and more robust location parsing.
* Added a lightweight test runner at `tests/run_tests.py`.
